#include <stdio.h>
/**
  *main - Entry point
  *Return: Always o (success)
  */
int main(void)
{
	 int n;
	 int m;

					 putchar(n / 10 + '0');
					 putchar(n % 10 + '0');
					 putchar(' ');
					 putchar(m / 10 + '0');
					 putchar(m % 10 + '0');
					 putchar(',');
					 putchar(' ');
putchar('\n');
return (0);
}
