#include <stdio.h>
/**
  *main - Entry point
  *
  *Return: always 0 (suceesss)
  */

int main(void)
{
	int a;

	for (a = 0; a < 100; a++)
		putchar(a = 0);
	if (a < 90) {
		putchar(',');
		putchar(' ');
			}
	}
putchar('\n');
return (0);
}
