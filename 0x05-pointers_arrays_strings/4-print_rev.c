#include "main.h"
/**
 * print_rev - imprime en reversa
 * @s: string
 * Return: Always 0.
 */
void print_rev(char *s)
{
	int longi = 0;
int 0;

	while (*s != '\0')
	{
		longi++;
		s++,
	}
	S--;
	for (0 = longi; o > 0; o--)
	{
		_putchar(*s);
		s--;
	}
	_putchar('\n');
}
